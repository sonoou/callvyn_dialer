<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/notification_ongoing_container"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@drawable/bg_headsup_card"
    android:orientation="vertical"
    android:paddingStart="14dp"
    android:paddingEnd="14dp"
    android:paddingTop="12dp"
    android:paddingBottom="12dp">

    <!-- Top Section: Avatar + Header Text -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical">

        <!-- Avatar with Badge -->
        <FrameLayout
            android:layout_width="48dp"
            android:layout_height="48dp">

            <ImageView
                android:id="@+id/iv_caller_avatar"
                android:layout_width="48dp"
                android:layout_height="48dp"
                android:src="@mipmap/ic_launcher"
                android:scaleType="centerCrop" />

            <ImageView
                android:id="@+id/iv_badge_icon"
                android:layout_width="18dp"
                android:layout_height="18dp"
                android:layout_gravity="bottom|end"
                android:src="@mipmap/ic_launcher"
                android:scaleType="centerInside" />
        </FrameLayout>

        <!-- Header and Subtitle Texts -->
        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:layout_marginStart="12dp"
            android:orientation="vertical">

            <TextView
                android:id="@+id/tv_header_title"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Caller Name • Callvyn • 11:06 am"
                android:textColor="#FFFFFF"
                android:textSize="15sp"
                android:textStyle="bold"
                android:singleLine="true"
                android:ellipsize="end" />

            <TextView
                android:id="@+id/tv_subtitle"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="3dp"
                android:text="Ongoing call • 00:00"
                android:textColor="#34C759"
                android:textSize="13sp"
                android:singleLine="true"
                android:ellipsize="end" />
        </LinearLayout>
    </LinearLayout>

    <!-- Bottom Line: 3 Action Icon Buttons (Red End Call, Hold, Speaker) -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="10dp"
        android:gravity="end|center_vertical"
        android:orientation="horizontal">

        <!-- Speaker Button -->
        <ImageView
            android:id="@+id/btn_speaker_call"
            android:layout_width="42dp"
            android:layout_height="42dp"
            android:background="@drawable/bg_btn_action_circle"
            android:src="@drawable/ic_speaker_white"
            android:padding="10dp"
            android:scaleType="centerInside"
            android:contentDescription="Speaker" />

        <Space
            android:layout_width="16dp"
            android:layout_height="1dp" />

        <!-- Hold Button -->
        <ImageView
            android:id="@+id/btn_hold_call"
            android:layout_width="42dp"
            android:layout_height="42dp"
            android:background="@drawable/bg_btn_action_circle"
            android:src="@drawable/ic_hold_white"
            android:padding="10dp"
            android:scaleType="centerInside"
            android:contentDescription="Hold" />

        <Space
            android:layout_width="16dp"
            android:layout_height="1dp" />

        <!-- Red End Call Button -->
        <ImageView
            android:id="@+id/btn_end_call"
            android:layout_width="42dp"
            android:layout_height="42dp"
            android:background="@drawable/bg_btn_reject_circle"
            android:src="@drawable/ic_call_end_white"
            android:padding="9dp"
            android:scaleType="centerInside"
            android:contentDescription="End Call" />
    </LinearLayout>
</LinearLayout>
